VERDICT: PASS

CRITICAL FINDINGS (future-blocking — will require rearchitecture):
- None. The REUSE architecture (sources table as provenance spine, extending existing freshness_scan_log, routing through existing queues) is correct. No new tables means no migration debt. Daen's single-normalized-view + matview recommendations are the right call.

HIGH FINDINGS (accumulating debt — must address within 30 days):
- **P4 extraction wall (138 DTAs + 718 laws at 0% sourced).** At Varys's demonstrated throughput (~3-5 sources/day), P3 pricing (36 zones + 841 rows) takes 7-12 days. P4 DTAs (138 × ~45 min each) + laws (718 × ~30 min each) = ~463 person-hours ≈ 58 days if Varys does nothing else. The plan has no parallelism strategy, no priority ordering for the 718 laws, and no fallback if Varys is interrupted. By Month 2-3, the board shows 700+ entities in `unsourced` with no way to accelerate. Mitigation: declare a priority tier for the 718 laws (e.g., "active FTA rulings first, repealed laws last") and define a Phase-0 sampling plan to validate extraction time per category before committing to the full P4 timeline.
- **Legislation freshness = content_hash, but content_hash on WHAT?** The plan correctly switches legislation to content_hash-based freshness rather than clock-based cadence. But the plan doesn't specify what is hashed — the web fetch of the law text? The PDF download? The extracted fact set? From knowledge-engine-R1 (pattern: content_hash on wrong granularity → false positives from source reformats), if `content_hash` is set on the raw fetch/gazette page, any formatting change to the government gazette triggers a false stale flag for unrelated laws. The `freshness_scan_log` needs fact-level or law-level hashing, not page-level. Add a note specifying `content_hash = hash(canonicalized_law_identifier + last_known_article_count)` or equivalent per-law granularity before the scanner goes live.

MEDIUM FINDINGS (trajectory warnings — monitor):
- **freshness_scan_log has an unknown owning service.** Daen's Q4 is correct — the table exists but is dormant. If there's a partially-built scanner or competing schema lock, the "extend, don't build" assumption could break. Recommending a file-level or DB-catalog check before Phase 2 starts. If the table has migrations from a prior attempt, we need migration history, not just presence.
- **DTA scoring at 3/152 is the visible but misleading burn-down.** The board will show 149 DTAs in `sourced_unscored` within weeks (Varys can fetch docs). But scoring is gated on Vulcan (binding phase). Vulcan scored 3 DTAs in the pilot — scoring throughput is unknown for 149. If Vulcan's capacity is 3/week, P5 takes ~50 weeks. This doesn't block PASS because the plan correctly separates sourcing (P4) from scoring (P5), but Peter should understand the burn-down rate cliff: the first green DTAs arrive fast (Varys sources them), then the board plateaus at `sourced_unscored` while Vulcan scores. Mitigation: identify which DTAs are most frequently queried in advice so the first ~20 DTAs are score-prioritized to show meaningful green progress.
- **Varys single-extractor for P3 + P4.** Same pattern from knowledge-engine-R1 (single executor for 180+ extraction cycles). The P3-P4 extraction load alone is 2+ months of Varys's capacity. If Marcus, Kenji, or another agent can do extract-only work (find doc + link it in sources table — no scoring needed), the throughput doubles. Not a blocker now but will be by Month 2.

TIMELINE:
- Week 0-1: Phase 0 (Daen's open questions + view spec) — clear
- Week 1-2: Phase 1 (VIEW live) — clear
- Week 2-3: Phase 2 (scanner extended, last_fetched_at populated) — clear if freshness_scan_log is clean
- Month 1: Phase 3 (pricing burn-down) — completes within estimate IF Varys has no other work
- Month 2-3: Phase 4 DTAs (138 sourced) — starts to slow as extraction hits legislation overhead
- Month 3-5: Phase 4 legislation (718 laws) — extraction wall visible; board plateaus in `sourced_unscored`
- Month 5+: Phase 5 scoring — throughput-dependent on Vulcan; board shows green rate reflects scoring capacity, not extraction progress

NOTES:
- **Pattern watch: cross-source contradiction detection** (from DTA Source Scoring R1). The plan correctly focuses on provenance (state machine per entity with source→scored→stale lifecycle). But it doesn't address what happens when TWO sources are both `verified_current` and disagree. For DTAs where two government sources publish different interpretations (common in the first year of a new treaty), the view shows both as green. The contradiction is invisible at the provenance layer. This is a separate concern but worth noting for future: if the board shows all entities green and an advisor still gives wrong advice because two green sources contradicted each other, the provenance system is blamed even though it did its job. Add an optional "contradiction warning" to the board downstream.
- **Pattern match: knowledge-engine-R1 extraction calibration.** The legislation pipeline (718 laws, 0% sourced) has no extraction calibration phase. If Varys's extraction approach for laws differs from DTAs (gazettes vs treaty documents), a Phase 0 sample of ~10 laws to validate extraction time and approach would save ~2 weeks of wrong-direction work on P4.
- **Pattern match: cron collision (capability-map C2).** The plan doesn't specify when the freshness scanner runs. If it fires at 22:00 UTC (same as gcg-doc-index.timer and gcg-data-model-sync), there's a triple race condition on source-freshness reads. Serialize the scanner or run it at a non-standard time (e.g., 05:00 UTC).
- Daen's 4 open questions are well-targeted. Q3 (legislation cadence = content_hash, not clock) is architecturally correct and should be documented in the scanner spec explicitly. Recommend answering all 4 before Phase 1 build starts.
- **The plan is honest about the gap — no false optimism.** The baseline table (pulled 2026-06-06) shows exactly where each domain stands. This is the correct starting point for a burn-down. The phases are in the right order (tooling first, then extraction, then scoring). This plan will materially improve GCG's data trust by end of Month 1 (visibility) and continue improving through Month 6. I see a clean architectural trajectory. PASS.
